services:
  data-fetcher:
    image: alpine:latest
    container_name: ${PROJECT_NAME:-corpora}-data-fetcher
    volumes:
      - ../data:/data
      - ../backend:/src/backend:ro
      - ./preprocess.py:/src/preprocess.py:ro
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        apk add --no-cache curl unzip python3 >/dev/null

        if [ -z "$$(ls -A /data/*.json 2>/dev/null)" ]; then
          echo "Downloading corpus data from GitHub Release..."
          curl -fL -o /tmp/data.zip "https://github.com/lunadunkel/NivkhCorpus/releases/download/data-v1/data.zip"
          unzip -oj /tmp/data.zip -d /data
          rm -f /tmp/data.zip
        else
          echo "Corpus data already present in ./data — skipping download."
        fi

        if [ -f /data/mongo-ready/.done ]; then
          echo "Preprocessed data already present — skipping."
          exit 0
        fi

        echo "Preprocessing JSON before import..."
        python3 /src/preprocess.py
        touch /data/mongo-ready/.done
        echo "Done."

  mongo:
    image: mongo:4.4
    container_name: ${PROJECT_NAME:-corpora}-mongo
    command: ["mongod", "--quiet", "--logpath", "/dev/null"]
    restart: unless-stopped
    depends_on:
      data-fetcher:
        condition: service_completed_successfully
    volumes:
      - mongo_data:/data/db
      - ../data/mongo-ready:/data/import:ro
      - ./import.sh:/docker-entrypoint-initdb.d/import.sh:ro
    environment:
      MONGO_INITDB_DATABASE: corpus
    healthcheck:
      test: ["CMD", "mongo", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    container_name: ${PROJECT_NAME:-corpora}-app  
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:8000"
    depends_on:
      mongo:
        condition: service_healthy
    environment:
      MONGO_URI: mongodb://mongo:27017/corpus
      WAIT_ATTEMPTS: 150
      WAIT_INTERVAL: 4

volumes:
  mongo_data:
    name: ${PROJECT_NAME:-corpora}-mongo_data

networks:
  default:
    name: ${PROJECT_NAME:-corpora}-default